{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm install && pnpm prisma migrate deploy && pnpm prisma generate && pnpm exec tsx prisma/seed.ts && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "github": {
    "enabled": true,
    "silent": false
  }
}
